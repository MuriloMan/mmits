mixin itemDesc(itemId, imgName, imgTitle, projName, ...items)
    div(class="row d-none portfolioDetailItem" id=itemId)
        div(class="col-12")
            hr
        div(class="col-12 col-sm-3 d-flex")
            img(src="/assets/portfolio/" + imgName + ".jpg" title=imgTitle title-t=imgTitle class="w-100 my-auto")
        div(class="col-12 col-sm-9 d-flex")
            div(class="row")
                div(class="col-12")
                    h2(class="mt-3 font-weight-bold" data-t=projName)
                    hr
                div(class="col-12")
                    strong(class="font-weight-bold" data-t="projectsandtechs")
                        | Projects and Technologies used
                    each i in items
                        p(data-t=i)